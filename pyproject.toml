[project]
name = "linkedin-jobs"
version = "0.1.0"
description = "LinkedIn Jobs Tracker — fetch, classify and filter saved jobs"
requires-python = ">=3.12"
dependencies = [
    "click>=8.4.2",
    "playwright>=1.60.0",
    "pyyaml>=6.0.3",
    "requests>=2.34.2",
    "tabulate>=0.10.0",
]
